
  # Input form with results display

  This is a code bundle for Input form with results display. The original project is available at https://www.figma.com/design/KretwaGorHhqtUvSFRpdN6/Input-form-with-results-display.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  